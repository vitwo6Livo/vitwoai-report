import 'package:flutter/material.dart';

class FunctionalWiseScreen extends StatelessWidget {
  const FunctionalWiseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Functional Wise Screen'),
      ),
    );
  }
}
