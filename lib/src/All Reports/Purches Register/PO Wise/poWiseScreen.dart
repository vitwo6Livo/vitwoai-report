import 'package:flutter/material.dart';

class PoWiseScreen extends StatelessWidget {
  const PoWiseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Po Wise Screen'),
      ),
    );
  }
}
