import 'package:flutter/material.dart';

class VendorWiseScreen extends StatelessWidget {
  const VendorWiseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Vendor Wise Screen!"),
      ),
    );
  }
}
