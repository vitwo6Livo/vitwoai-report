import 'package:flutter/material.dart';

class SoWiseScreen extends StatelessWidget {
  const SoWiseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('So Wise Screen'),
      ),
    );
  }
}
