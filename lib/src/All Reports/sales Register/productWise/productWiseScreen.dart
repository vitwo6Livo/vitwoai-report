import 'package:flutter/material.dart';

class ProductWiseReceiveableScreen extends StatelessWidget {
  const ProductWiseReceiveableScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Product Wise Receiveable Screen'),
      ),
    );
  }
}
