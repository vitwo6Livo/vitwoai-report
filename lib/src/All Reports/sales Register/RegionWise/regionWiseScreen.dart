import 'package:flutter/material.dart';

class RegionWiseScreen extends StatelessWidget {
  const RegionWiseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Region Wise Screen'),
      ),
    );
  }
}
