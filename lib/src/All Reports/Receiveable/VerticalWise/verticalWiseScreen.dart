import 'package:flutter/material.dart';

class VerticalWiseScreen extends StatelessWidget {
  const VerticalWiseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Vertical Wise Screen'),
      ),
    );
  }
}
