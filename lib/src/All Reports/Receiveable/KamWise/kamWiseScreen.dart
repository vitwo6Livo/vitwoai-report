import 'package:flutter/material.dart';

class KamWiseScreen extends StatelessWidget {
  const KamWiseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Kam Wise Screen'),
      ),
    );
  }
}
