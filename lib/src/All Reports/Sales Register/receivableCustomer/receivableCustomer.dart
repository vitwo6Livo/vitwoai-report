import 'package:flutter/material.dart';

class ReceivableCustomerScreen extends StatelessWidget {
  const ReceivableCustomerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Receivable Customer Screen'),
      ),
    );
  }
}
