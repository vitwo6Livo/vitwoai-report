import 'package:flutter/material.dart';

class PurchaseOrderGraph extends StatelessWidget {
  const PurchaseOrderGraph({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: double.infinity,
      child: Center(
        child: Text('Purchase Order Wise Graph View'),
      ),
    );
  }
}
